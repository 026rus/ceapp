<PreferenceScreen xmlns:android = "http://schemas.android.com/apk/res/android">

	<!--
	<SwitchPreference
		android:defaultValue = "true"
		android:key = "example_switch"
		android:summary = "@string/pref_description_gps"
		android:title = "@string/pref_title_gps"/>
-->
	<!-- NOTE: EditTextPreference accepts EditText attributes. -->
	<!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
	<EditTextPreference
		android:capitalize = "words"
		android:inputType = "textCapWords"
		android:key = "@string/pref_saved_name"
		android:maxLines = "1"
		android:selectAllOnFocus = "true"
		android:singleLine = "true"
		android:title = "@string/pref_title_display_name"/>

	<EditTextPreference
		android:inputType = "phone"
		android:key = "@string/pref_saved_phone_number"
		android:maxLines = "1"
		android:selectAllOnFocus = "true"
		android:singleLine = "true"
		android:title = "@string/pref_title_tech_phone"/>

	<!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
		android:defaultValue = "@string/pref_default_display_phone"
		android:defaultValue = "@string/pref_default_display_name"
         dismiss it. -->

</PreferenceScreen>
